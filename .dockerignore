# Dependencies
node_modules
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build output
.next
out
build
dist

# Local environment files (NEVER ship to image)
.env
.env.local
.env.*.local
.env.production
.env.development

# Sidecar env files
sidecar/.env
sidecar/.env.*

# Git
.git
.gitignore
.gitattributes

# Editor/IDE
.vscode
.idea
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log
logs

# Test
coverage
__tests__
.vitest

# Local databases (NEVER ship to image)
*.db
*.db-journal
*.sqlite
*.sqlite3
prisma/dev.db
dev.db

# Docs (don't need in container)
docs

# Fly.io artifacts (we're not using Fly)
fly.toml
fly.sidecar.toml
.fly

# Deploy directory (this is config, not app code)
deploy

# Uploads directory (user content lives outside container)
uploads

# Temp/scratch
tmp
temp
*.tmp

# CI/CD
.github

# Existing artifacts
tsconfig.tsbuildinfo
