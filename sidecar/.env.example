# Shared secret between Next.js and sidecar — must match SIDECAR_API_KEY in .env.local
SIDECAR_API_KEY=change-me-to-a-random-secret

# Anthropic API key for AI-enhanced parsing (optional — only needed for /parse/specs/ai)
ANTHROPIC_API_KEY=sk-ant-...
